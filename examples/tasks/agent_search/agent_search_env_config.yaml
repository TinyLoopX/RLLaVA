env:
  class_name: "./examples/tasks/agent_search/agent_search_env.py:AgentSearchEnv"
  config:
    backend: bocha          # one of: bocha / serper / ddg
    search_num: 4
    search_mode: fast       # fast | pro (matches eval_mat_search.py)
    concurrent_workers: 4
    # bocha_api_key: "sk-..."  # optional override for the BOCHA API key
